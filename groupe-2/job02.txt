
grep 'tail -f /dev/null' ps.txt         =       afficher la ligne contenant "tail -f /dev/null" du fichier `ps.txt`

grep -v 'root' ps.txt                   =       n'afficher que la ligne ne contenant pas "root" du fichier

awk '{print $1}' ps.txt                 =       n'afficher que la première colonne du fichier
